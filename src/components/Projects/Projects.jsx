import { ArrowUpRight, ExternalLink } from 'lucide-react';
import { projects } from '../../data/projects.js';
import { ScrollReveal } from '../ScrollReveal/ScrollReveal.jsx';
import './Projects.css';

export function Projects() {
  return <section id="projects" className="projects-section"><div className="section-shell section-block"><div className="section-kicker"><span>04</span><p>Selected work</p></div><div className="projects-editorial-intro"><div><p className="eyebrow">A few things taking shape</p><h2>Work in <em>progress.</em></h2></div><p>These are honest concept projects while I build out the public archive. The structure is ready for real work to take their place.</p></div><div className="projects-grid projects-editorial">{projects.map((project, index) => <ScrollReveal key={project.title} delay={index * 90}><article className={`project-editorial project-card ${project.tone}`}><div className="project-art"><img className="project-preview" src={project.image} alt={`${project.title} concept preview`} loading="lazy" /><div className="project-art-top"><span>{project.number} / concept</span><ExternalLink size={18} /></div><span className="art-label">{project.technologies[0]} / {String(index + 1).padStart(2, '0')}</span></div><div className="project-details"><div><p className="project-category">{project.technologies.join(' · ')}</p><h3>{project.title}</h3><p>{project.description}</p><a href="#contact" className="project-text-link">Discuss this project <ArrowUpRight size={16} /></a></div><a href="#contact" className="project-link" aria-label={`Discuss ${project.title}`}><ArrowUpRight size={18} /></a></div></article></ScrollReveal>)}</div></div></section>;
}
