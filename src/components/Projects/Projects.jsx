import { ArrowUpRight, ExternalLink } from 'lucide-react';
import { projects } from '../../data/projects.js';
import { ScrollReveal } from '../ScrollReveal/ScrollReveal.jsx';
import './Projects.css';

export function Projects() {
  return <section id="projects" className="projects-section"><div className="section-shell section-block"><div className="section-kicker"><span>05</span><p>Selected work</p></div><div className="projects-editorial-intro"><div><p className="eyebrow">Dental / healthcare / real references</p><h2>Work that <em>matters.</em></h2></div><p>A selection of public dental website references. The previews are local presentation placeholders until the final screenshots are supplied.</p></div><div className="projects-grid projects-editorial">{projects.map((project, index) => <ScrollReveal key={project.title} delay={index * 90}><article className={`project-editorial project-card ${project.tone}`}><div className="project-art"><img className="project-preview" src={project.image} alt={`${project.title} website preview placeholder`} loading="lazy" /><div className="project-art-top"><span>{project.number} / {project.category}</span><ExternalLink size={18} /></div><span className="art-label">{project.technologies[0]} / {project.role}</span></div><div className="project-details"><div><p className="project-category">{project.role} · {project.technologies.join(' · ')}</p><h3>{project.title}</h3><p>{project.description}</p><a href={project.url} target="_blank" rel="noopener noreferrer" className="project-text-link">View website <ArrowUpRight size={16} /></a></div><a href={project.url} target="_blank" rel="noopener noreferrer" className="project-link" aria-label={`View ${project.title} website`}><ArrowUpRight size={18} /></a></div></article></ScrollReveal>)}</div></div></section>;
}
